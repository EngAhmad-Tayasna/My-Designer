import 'package:flutter/material.dart';

class ChooseDressColor extends StatelessWidget {
  const ChooseDressColor({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
